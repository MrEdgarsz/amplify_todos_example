import 'package:flutter/material.dart';

const Duration animateToPageDuration = Duration(milliseconds: 300);
const Duration debounceDuration = Duration(milliseconds: 500);
const Curve animateToPageCurve = Curves.easeInOut;
const int changePagePaddingThreshold = 60;
const Duration testServiceRequestDuration = Duration(milliseconds: 200);
